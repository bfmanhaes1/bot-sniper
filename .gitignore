# Nunca comitar credenciais reais nem logs
secrets.json
.env
logs/
*.log
__pycache__/
*.pyc
.maintenance
managed_positions.json

# Logs diários do modo sombra (efêmeros; resumo durável vai ao Telegram)
crypto2_logs/*.json
crypto2_logs/*.md
GUIA_CONFIRMACAO_SNIPER.docx_preview/

# Artefato local do estudo (não versionar)
estudo_tpsl.jsonl
crypto2_logs/

# Estado de runtime do agente self-learning (efemero)
agent/agent_state.json
*.tmp
